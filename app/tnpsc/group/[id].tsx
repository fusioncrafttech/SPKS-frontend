import { Brand } from '@/constants/brand';
import { GroupHubScreen } from '@/components/group-hub';

export default function GroupDetailScreen() {
  return <GroupHubScreen courseSlug="tnpsc" gradient={Brand.indigoSoft} />;
}
